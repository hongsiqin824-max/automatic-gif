# 后续优化清单

本清单记录当前已确认、但暂不影响主流程的优化项。加入清单不代表立即修改，实施前需要先确认产品契约和兼容范围。

## 1. TimelineState 的 origin 更新策略

- **优先级**：中
- **位置**：`pipeline_runtime.py` 的 `upsert_timeline_state()`
- **现状**：`ON CONFLICT` 分支会直接更新 `timeline_origin_wall_unix` 和 `timeline_origin_stream_time`。
- **风险**：同一 match 重复 upsert 时，如果传入不同 origin，已有的 wall time/stream time 换算坐标可能整体漂移；两个 origin 必须作为一对处理。
- **当前影响**：现有主流程通常会复用已保存的 origin，并在 manifest 与数据库不一致时拒绝恢复，因此暂未观察到必现故障。
- **后续处理**：先明确 origin 是“创建后不可变”，还是允许显式重新校准；随后补充成对更新、manifest 同步和重复 upsert 的回归测试。

## 2. legacy scoreboard 路径的降级模式参数

- **优先级**：中（仅在 legacy 路径仍受支持时）
- **位置**：`live_goal_pipeline.py` 的 scoreboard 调用链
- **现状**：legacy scoreboard 路径未传递 `allow_degraded=True`。
- **风险**：在降级条件下，旧路径可能因为未开启降级模式而拒绝继续处理或表现与新版 event-driven 路径不一致。
- **当前影响**：当前 Dashboard 使用 `event_driven_pipeline.py`，因此主流程不受影响；如果旧脚本仍被直接调用或作为回退方案，风险仍然存在。
- **后续处理**：确认 legacy 脚本是否继续对外支持；若继续支持，统一参数传递并补充降级场景测试；若已废弃，则在文档中明确边界。

## 3. 普通时间/补时 ROI 分类缓存与当前事件自动重定位

- **状态**：待实施
- **优先级**：高（先实施当前事件重定位，再实施分类缓存）
- **范围**：仅 OCR 精确定位链路；默认 GIF 不得等待或依赖本功能
- **主要位置**：`pipeline_runtime.py`、`vision_runtime.py`、`scoreboard_ocr_worker.py`、`test_ocr_budget_roi_cache.py`、`test_vision_runtime.py`

### 3.1 现状与问题

- `scoreboard_roi_cache` 目前以 `match_id` 作为唯一主键，每场比赛只能保存一份 ROI。
- 普通时间的时钟常为 `42:17`，补时可变为 `90+2`、`90+5`，记分牌宽度和时钟位置可能同时改变。
- 已观察到比赛 `54355915` 的普通 ROI 约为 `[186, 91, 304, 125]`，`90+2` 的自动发现 ROI 约为 `[104, 96, 385, 128]`，补时布局明显更宽。
- 已观察到比赛 `54480314` 在 `90+5` 目标附近使用旧 ROI 出现 `clock_profile_mismatch`。
- 当带缓存 profile 的当前 OCR 请求失配时，代码会记录失败并使缓存失效，但只允许下一个请求无缓存重新发现 ROI；当前事件仍可能直接进入后续降级或失败。
- 普通时间也出现过 profile 失配，因此“只分成普通/补时两份缓存”不是完整解法，必须同时实施当前事件的有界重定位。

### 3.2 目标契约

1. 事件进入 OCR 时，根据 `minute_extra` 或事件分钟中的 `+` 得到 `layout_mode`：`normal` 或 `stoppage`。
2. 自动 ROI 缓存按 `match_id + layout_mode + resolution_key` 隔离，不再让普通时间和补时相互覆盖。
3. 当自动缓存 profile 触发 `clock_profile_mismatch` 时，当前事件立即去掉缓存 profile，在相同已租用视频窗口上自动发现 ROI 并重试一次。
4. 自动重定位每个事件最多一次，尝试次数持久化，进程重启不得导致无限重试。
5. 重定位成功后，仅更新当前 `layout_mode + resolution_key` 的自动缓存；不覆盖其他布局的缓存。
6. 人工明确配置的 `scoreboard_profile` 保持最高优先级，失配时不允许自动覆盖或重定位。
7. 两次 OCR 都失败后才进入现有降级语义；不改动 OCR/T-DEED/分钟级 GIF/最终失败的现有优先级。
8. 默认 GIF 仍按原有独立任务立即生成，不等待 ROI 查找、OCR 或重定位。

### 3.3 数据结构与迁移方案

- 新增版本化缓存表，建议名为 `scoreboard_roi_cache_v2`，联合主键为 `(match_id, layout_mode, resolution_key)`。
- 字段至少包含 `profile_json`、`confidence`、`success_streak`、`failure_streak`、`updated_at_unix`；`layout_mode` 只接受 `normal/stoppage`。
- `resolution_key` 使用 OCR 实际分析画面尺寸，格式为 `WIDTHxHEIGHT`，例如 `1920x1080`，不使用未验证的接口字段。
- 读取时优先选择相同布局和完全相同分辨率的记录；当本次分辨率尚不可知时，可暂用当前布局最新记录，交由 worker 的分辨率/内容质量验证把关。
- 旧表中的 profile 可能来自普通时间，也可能来自补时，不能无条件迁移为 `normal`。新代码只读 v2 表，旧表暂时保留但不再读取，等新缓存稳定后再单独清理，避免破坏性迁移。
- 失败计数只更新当前键；`clock_profile_mismatch` 立即使当前键失效，不影响同场比赛的另一种布局。

### 3.4 当前事件自救流程

```text
解析 layout_mode
  -> 查找对应布局/分辨率的自动 ROI 缓存
  -> 有缓存：使用缓存执行 OCR
       -> 成功：增加当前缓存成功计数
       -> clock_profile_mismatch：
            1. 保存原始失败诊断
            2. 持久化 roi_rediscovery_attempt_count = 1
            3. 将 scoreboard_profile 置空
            4. 在当前事件的同一视频窗口上重试一次
            5. 成功则保存新 ROI 并继续精确定位
            6. 失败则记录第二次失败，进入现有降级链路
  -> 无缓存：按现有方式自动发现 ROI，成功后写入对应缓存
```

实施时的关键约束：

- 只有 `_job_with_cached_scoreboard_profile()` 实际注入的自动缓存才可触发重定位；以此区分自动 profile 和人工 profile。
- 重试前必须先将尝试次数写入 `ocr_window` 任务的 `progressive_scan` 状态，不能只保存在内存里。
- 重试复用当前的切片租约和候选窗口，不重新触发默认 GIF，不改写事件时间。
- 初次失配诊断和重定位诊断必须分开保存，不得用最终结果覆盖第一次失败原因。
- 第一版只对 `clock_profile_mismatch` 开启当前事件重定位。`ocr_clock_unreadable` 和 `scoreboard_missing` 可能是无记分牌或源画面问题，不盲目增加第二次昂贵 OCR；待诊断样本足够后再评估扩展。

### 3.5 诊断与 Dashboard 呈现

每个 OCR 任务应保存以下字段：

- `scoreboard_layout_mode`：`normal` 或 `stoppage`。
- `scoreboard_cache_resolution_key`：本次命中或写入的分辨率键。
- `scoreboard_cache_status`：`miss`、`reused`、`discovered`、`invalidated`、`replaced`。
- `scoreboard_cached_profile_id`：实际使用的缓存 profile ID。
- `roi_rediscovery_attempt_count`：当前事件已尝试次数，最大为 1。
- `roi_rediscovery_trigger`：首次 OCR 失配的 kind 和 message。
- `roi_rediscovery_status`：`not_needed`、`running`、`succeeded`、`failed`、`skipped_explicit_profile`。
- `roi_rediscovery_diagnostics`：第二次 OCR 的独立诊断。

Dashboard 对应展示为：

- 成功：“缓存的记分牌布局已失效，本事件自动重定位成功”。
- 失败：“缓存布局失配，已在当前事件重新查找，但仍未找到可用时钟”，并继续展示后续降级结果。
- 人工 profile 失配：“人工配置的记分牌区域与当前画面不匹配，未自动覆盖配置”。

### 3.6 实施顺序

1. 新增 `layout_mode` 解析函数和单元测试，不改变现有运行分支。
2. 先实施当前事件一次性重定位及持久化次数，解决“要等下一个事件才自愈”的核心问题。
3. 新增 v2 缓存表和按键读写 API，保留旧表，用存储层测试验证隔离。
4. 将 OCR 主链路切换到 v2 缓存，将自动发现结果写入对应布局和分辨率。
5. 增加诊断字段和 Dashboard 文案，确保可区分“首次失配”、“重定位成功”和“两次均失败”。
6. 使用保存的普通时间、`45+N`、`90+N` 真实视频做回放对比，通过后再在生产 AI 精剪路径启用。
7. 稳定运行一段时间后，根据命中率、重定位成功率和额外 OCR 耗时，再决定是否清理旧缓存表或扩展重试错误类型。

### 3.7 必须覆盖的测试

- `normal`、`45+2`、`90+5` 及异常 `minute_extra` 的布局分类。
- 同一 `match_id` 的普通/补时缓存可同时存在，成功和失败计数互不影响。
- 同一布局不同分辨率的缓存不互相覆盖。
- 旧表存在时数据库可正常启动，且旧 profile 不会被错误归类为 `normal`。
- 自动缓存首次 `clock_profile_mismatch` 后，当前事件确实以 `scoreboard_profile=None` 重试一次。
- 重定位成功后正常输出 OCR GIF，并只更新当前布局/分辨率缓存。
- 重定位再次失败时不第三次重试，两次诊断均被保留，并进入现有降级路径。
- 进程在重定位过程中重启后，持久化次数阻止无限重试。
- 人工 profile 触发失配时不进行自动重定位，配置内容不被覆盖。
- 默认 GIF 在 OCR 重定位成功、失败或超时的所有场景中都可以独立生成。
- 现有 OCR/T-DEED/分钟级 GIF/失败语义的回归测试全部通过。

### 3.8 验收标准和预期效果

- 首次进入补时布局时，即使误用了普通时间 ROI，当前事件也能立即重新查找 ROI，不再必须等下一个事件。
- 同场比赛后续的普通时间和补时事件各自复用对应 ROI，降低重复自动搜索和布局串用。
- 不承诺所有记分牌都必然识别成功；无记分牌、画面遮挡、源切换、OCR 模型无法读数等问题仍可能失败，但失败阶段和两次尝试原因必须明确可见。
- 对首次失配的事件会增加一次 OCR 计算和处理时间；通过“仅自动缓存失配”和“每事件最多一次”控制资源上限。
- 默认 GIF 时延和成功率不应因此变化；若回放或生产监控发现默认 GIF 任务受 OCR 线程/进程资源竞争影响，本功能不得直接全量开启。

### 3.9 待办登记

- **待办名称**：OCR 记分牌布局隔离与当前事件自动重定位
- **登记状态**：待实施，当前仅完成方案设计；本次登记不代表已修改运行代码。
- **目标**：解决普通时间与补时共用 ROI 导致的 `clock_profile_mismatch`，并让当前事件在首次缓存失配时立即自救，不必等待后续事件。
- **实施边界**：只修改 OCR 第二条链路及其缓存、诊断和测试；默认 GIF 第一条链路不等待、不依赖、不读取重定位结果；T-DEED 第三条链路保持关闭状态。

#### 执行步骤

1. **先加分类逻辑和测试**：解析 `normal/stoppage`，覆盖普通时间、`45+2`、`90+5` 和异常补时格式；此阶段不改变线上分支。
2. **实现当前事件一次性重定位**：缓存 ROI 失配时，在同一租约和同一候选窗口内清除自动 profile、重新发现并重试一次；把尝试次数写入任务状态，重启后仍最多一次。
3. **增加 v2 缓存存储**：以 `match_id + layout_mode + resolution_key` 隔离缓存，旧表保留但不迁移、不删除，避免历史数据被错误归类。
4. **切换 OCR 读写路径**：自动发现结果只写入对应布局和分辨率；人工 `scoreboard_profile` 继续拥有最高优先级，失配时不自动覆盖。
5. **补充可观测性**：记录缓存命中/失效、首次失败、重定位状态、第二次诊断和最终降级原因，并在 Dashboard 区分成功、重定位失败和人工配置失配。
6. **回放验证后再上线**：用普通时间、`45+N`、`90+N` 的保存视频验证，再进行小流量生产观察；确认默认 GIF 成功率、耗时和资源占用没有回归后再扩大范围。

#### 完成条件

- 当前事件的自动缓存失配最多触发一次重定位，成功后能继续生成 OCR GIF。
- 重定位失败不会第三次重试，首次和第二次原因都能查询，并进入既有降级语义。
- 普通时间和补时缓存互不覆盖，同布局不同分辨率也互不覆盖。
- 服务重启不会重置重定位次数，不会形成无限重试。
- 默认 GIF 在 OCR 重定位成功、失败、超时的场景中都能独立执行，现有默认 GIF 回归测试通过。

#### 风险控制与回滚

- 只在 `clock_profile_mismatch` 且命中自动缓存时触发重定位，控制额外 OCR 成本；无记分牌、画面遮挡等错误沿用现有失败/降级路径。
- 分阶段提交：分类逻辑、重定位、缓存 v2、诊断展示分别可回滚。
- 发现默认 GIF 受资源竞争影响时，立即关闭 OCR 重定位开关；保留旧缓存表和上一版本服务，恢复不需要删除历史 GIF 或任务记录。

## 4. 客观描述性文章标题与发布前编辑

- **状态**：待实施；等 GIF 生成、发布、授权和线上部署链路全部稳定后再开始
- **优先级**：中
- **范围**：文章标题生成、Dashboard 正式发布交互、发布接口校验和发布记录；不得改变 GIF 生成、OCR、T-DEED 或事件去重逻辑
- **主要位置**：`article_publisher.py`、`dashboard_server.py`、`dashboard_static/app.js`、相关发布测试

### 4.1 现状

- `build_article_title()` 当前使用固定模板：`{分钟}，{球员或球队}{事件类型}，{主队} {比分} {客队}`。
- 正式发布和 OCR 草稿共用该函数，Dashboard 只提交 `match_id` 和 `event_key`，没有自定义标题字段。
- 发布成功后按比赛、事件和 GIF 保持幂等；重复点击会返回原发布记录，不应因为修改标题而重复创建文章。

### 4.2 目标契约

1. 使用现有结构化数据生成客观标题，不调用语言模型，不根据 GIF 画面猜测动作。
2. Dashboard 在正式发布前展示可编辑标题，默认值为自动标题；用户可以直接使用，也可以修改后发布。
3. 自动标题信息不足时必须退回现有稳定模板，缺少富标题字段不得阻塞发布。
4. 第一版只自动描述可以验证的事实：比分状态、球员、进球/点球/乌龙、`shot_type=head` 头球以及可靠的定位球类型。
5. “神仙球”“背身凌空”“后点包抄”“绝杀”“绝平”等主观或需要观看画面、等待终场才能确认的描述，不得自动生成，只允许人工填写。
6. 最终标题去除首尾空白，不能为空，最长 100 个字符；拒绝控制字符和纯 HTML 内容，正文继续进行 HTML 转义。
7. 发布请求不传标题时仍按旧方式自动生成，保证旧 Dashboard、脚本和已有调用兼容。
8. 成功发布后保存最终标题及标题来源 `auto/manual`。同一幂等键再次提交不同标题时返回原记录并明确“已经发布”，不更新或重复发布文章。

### 4.3 客观标题规则

进球标题优先使用以下顺序：

1. 事件动作：`PG/penalty` 为“点球破门”，`OG` 为“乌龙球”，`shot_type=head` 为“头球破门”，其余为“破门”。
2. 比分语义：只有比分格式有效、进球方能可靠映射为主队或客队时才计算。
3. 人物：优先使用 overview 补充后的球员姓名；缺失时使用事件球队；两者都缺失时省略人物，不展示球员 ID 或球队 ID。
4. 比分尾部：建议保留双方队名和完整比分，格式为 `{主队} {主队比分}-{客队比分} {客队}`，避免“萨索洛1-1”无法判断对手。

比分语义按进球后的比分反推：

- 进球后双方相等：`扳平比分！`
- 进球方进球后仍落后：`扳回一城！`
- 进球前平局、进球后领先：`取得领先！`
- 进球方原本领先、进球后扩大优势：`扩大领先！`
- 数据不足或存在矛盾：不添加比分语义，不猜测。

示例：

```text
扳平比分！奥登塔尔头球破门，萨索洛 1-1 对手
扳回一城！切-亚当斯破门，都灵 1-2 对手
点球破门！奥登塔尔主罚命中，萨索洛 2-1 对手
```

红黄牌继续使用客观模板，例如 `奥登塔尔吃到黄牌，萨索洛对阵对手`，不套用进球比分语义。

### 4.4 实施步骤

1. 新增纯函数形式的客观标题生成器及比分状态判断函数；保留当前 `build_article_title()` 作为兼容兜底。
2. 确认发布时使用最新合并后的事件数据。shotmap 事件尚未由 overview 补齐球员、球队或比分时，只生成可证明的部分。
3. 为正式发布 API 增加可选 `title` 字段，并在任何 GIF 复制或外部平台请求前完成类型、长度和内容校验。
4. 为发布器增加可选最终标题参数；没有人工标题时使用自动标题。保持现有幂等键不变，避免标题变化制造重复文章。
5. Dashboard 在每个可发布的默认 GIF 旁展示标题输入框和发布按钮；事件数据更新且用户尚未编辑时可刷新自动标题，一旦人工编辑便不得被后台刷新覆盖。
6. 发布记录增加兼容性字段，至少保存最终 `title` 和 `title_source`；旧数据库采用增量迁移，不删除历史记录。
7. OCR 草稿第一阶段继续保持当前自动行为；是否同步使用客观标题单独验证，避免正式发布交互改动影响草稿队列。
8. 增加功能开关并默认关闭；完成单元测试、Dashboard 联调和测试账号真实发布验证后再启用。

### 4.5 必须覆盖的测试

- 扳平、仍落后、首次领先、扩大领先以及球队方向相反时的比分语义。
- `G`、`PG`、`OG`、头球、普通进球、红牌和黄牌标题。
- 球员、球队、比分、shotmap 信息分别缺失时均能安全兜底，且不输出数字 ID。
- 人工标题的空值、超长、控制字符、HTML 和正常中文校验。
- 旧请求不传 `title` 仍可发布；人工标题能够原样保存并安全写入正文。
- 双击发布和成功后更改标题重试不会创建第二篇文章。
- Dashboard 自动标题刷新不会覆盖已经编辑的内容。
- 正式发布改动不影响默认 GIF、OCR GIF、OCR 草稿和事件任务状态。

### 4.6 验收与回滚

- 自动标题只能包含结构化数据可证明的信息，不出现主观夸张或动作误判。
- 用户在点击正式发布前可以看到并修改最终标题，发布记录能区分自动标题和人工标题。
- 信息不完整时仍能得到旧模板级别的有效标题，文章发布成功率不得降低。
- 关闭功能开关后恢复旧标题和旧 Dashboard 发布方式；数据库新增字段保留，不做破坏性回滚。

## 5. 可选关闭默认 GIF，保持 OCR 60 秒 GIF 独立运行

- **状态**：待实施；本次仅登记方案，未修改运行代码
- **优先级**：高（仅当最终只发布 OCR GIF，且需要降低服务器负载时启用）
- **目标**：关闭第一条“事件时间默认 GIF”编码，继续完整执行第二条“时钟 OCR 60 秒 GIF”链路；已有默认 GIF 文件和历史记录不得删除。
- **范围**：默认 GIF 提交、恢复、收尾、统计和 Dashboard 展示；OCR 事件记录、视频缓存/租约、OCR 定位、60 秒编码、草稿队列和发布流程保持不变。

### 5.1 当前核查结论

当前代码已经具备部分隔离：`vision_artifact_ready_for_submission()` 不检查默认任务状态；OCR 任务创建、shotmap 秒数升级、OCR 编码和发布均使用独立的 `ocr_window` 任务。现有独立性回归测试通过。

但仓库目前**没有真正的默认 GIF 开关**。默认任务仍在以下路径调用 `encode_event_job()`：启动恢复、实时调度、进程收尾和最终补偿。因此只在一个入口设置“关闭”会留下隐性编码或未完成状态，不能直接宣称已经安全。两条链路还共享 FFmpeg、CPU、内存、磁盘和全局重任务槽；关闭默认 GIF通常会降低竞争，但不会改变 OCR 模型本身的推理耗时。

### 5.2 配置契约

新增全局配置，缺省保持旧行为：

```env
DEFAULT_GIF_ENABLED=true
```

Dashboard 启动 Worker 时将该配置明确传为 `--default-gif-enabled` 或 `--disable-default-gif`。不使用 `gif_workers=0` 伪装关闭，因为现有参数校验要求正数，而且可能留下 pending 任务。

### 5.3 安全实施步骤

1. **增加配置和 CLI 透传**：默认值为 `true`；在 session JSON 和 Worker 命令中同时暴露“已配置/实际生效”状态。旧命令不带参数时行为完全不变。
2. **封装默认提交闸门**：实时循环只在开关开启时提交 `encode_event_job`；启动恢复、终场补偿和最终收尾使用同一闸门，确保没有旁路。
3. **保留事件父任务**：即使默认关闭，仍执行 `discover_task()`、事件修订、shotmap 更新和 OCR `enqueue_vision_task()`。不要删除或跳过父任务，否则 OCR 无法读取事件元数据。
4. **持久化可解释的终态**：第一版不改 SQLite `event_tasks.status` 的 CHECK 约束；默认关闭时记录 `status=failed`、`last_error_kind=default_gif_disabled`，结果中写入 `default_gif_disabled=true` 和 `reason=disabled_by_configuration`。Dashboard 将其显示为“默认 GIF 已关闭”，不计入失败或 `completed_with_warnings`。
5. **同步内存状态**：禁用任务不得进入默认 `jobs` 编码队列；若仍保留内存对象，调度器必须检查持久化状态，避免仅改数据库后又被提交。
6. **隔离恢复和收尾**：开关关闭时不恢复旧的默认编码任务；OCR 恢复、租约续期、取消和终态写入照常执行。已有 `encoded` 默认 GIF 保留，不能被清理逻辑误删。
7. **保护 OCR 资源**：关闭默认 GIF 后，`GIF_MAX_CONCURRENT_HEAVY_TASKS=1` 仍可运行 OCR，但多个 OCR 任务会串行，不能把它误认为并行配置；启动时记录实际槽位和预期吞吐，若需要并行则配置至少 2 个槽位。默认关闭 + OCR 开启不应因为槽位数为 1 被无条件拒绝。
8. **更新 Dashboard**：默认列显示“已关闭”且不出现红色失败；OCR 列继续显示定位、编码、失败原因和发布按钮。默认关闭时 OCR 失败必须明确说明“无默认 GIF 兜底”，但不能改变 OCR 的失败/重试语义。

### 5.4 必须覆盖的验收测试

- `DEFAULT_GIF_ENABLED=true` 与当前版本行为一致。
- 关闭时四条默认编码路径均不调用 `encode_event_job`，且不产生新的默认 GIF。
- 关闭时仍创建事件父任务和 `ocr_window`，OCR 可完成定位、60 秒编码、草稿入队和发布。
- shotmap 秒数到达后仍能升级 OCR 目标；不读取或修改默认 GIF状态。
- 重启不会恢复默认编码，但会恢复 OCR；已有默认 GIF 文件保持可访问。
- 默认关闭记录不计入失败数、健康告警和 `completed_with_warnings`。
- `GIF_MAX_CONCURRENT_HEAVY_TASKS=1` 时验证 OCR 可以串行完成且不会死锁；`=2` 及以上时验证当前保守预留策略是否造成视觉吞吐下降，并记录实际并发槽位。
- OCR 成功、OCR 失败、超时和比赛收尾四种场景均有清晰 Dashboard 文案。
- 使用真实保存视频回放，比较关闭前后的 OCR 成功率、耗时、缓存租约和发布结果。

### 5.5 预期效果、代价与回滚

- **预期**：不再为默认 GIF消耗 FFmpeg 编码、默认任务队列和对应磁盘写入；OCR 逻辑和 60 秒窗口不变，通常能减少资源竞争。
- **代价**：OCR 失败时没有默认 GIF 兜底；OCR 推理本身不会因为关闭默认编码而必然变快。
- **回滚**：将 `DEFAULT_GIF_ENABLED` 改回 `true` 并重启 Worker 即恢复旧行为；保留数据库、OCR 状态、缓存和历史默认 GIF，不执行破坏性迁移。上线前必须先通过上述测试和小流量回放，不能仅凭 Dashboard 显示“已关闭”判定完成。

## 6. 秒级进球 OCR GIF 改为前后 20 秒

- **状态**：待实施；本次仅登记需求和实施方案，未修改运行代码
- **优先级**：中高（属于低风险、可直接减少编码时间和文件体积的优化）
- **需求来源**：进球事件接口已经提供精确到秒的比赛时间时，用户希望最终 GIF 覆盖事件前 20 秒到事件后 20 秒，总时长约 40 秒。
- **目标**：在 OCR 已经找到比赛时钟锚点后，把秒级进球 GIF 的输出窗口从当前 `30/30` 秒调整为 `20/20` 秒。
- **主要位置**：`vision_runtime.py`、`dashboard_server.py`、必要时 `event_driven_pipeline.py` 的任务初始元数据，以及 `test_vision_runtime.py`、`test_vision_output_contract.py`、`test_dashboard_server.py` 和相关事件任务测试。

### 6.1 当前现状

- `vision_runtime.py` 当前通过 `OCR_EXACT_WINDOW_BEFORE_SECONDS = 30.0` 和 `OCR_EXACT_WINDOW_AFTER_SECONDS = 30.0` 定义秒级 OCR 输出窗口，因此最终通常生成约 60 秒 GIF。
- OCR 定位成功后，`_ocr_output_shape()` 根据 `location_kind` 选择窗口，随后在 OCR 任务收尾阶段重新计算并持久化 `clip_before_seconds`、`clip_after_seconds`，编码范围是 `anchor - before` 到 `anchor + after`。
- `match_clock_second` 既可能代表 OCR 直接读到目标秒，也可能代表单帧读到、附近读数估算或稳定时钟映射推算；窗口调整不改变这些结果的 `exact`、`estimated`、`projected` 诊断含义。
- `match_clock_minute_boundary`、`match_clock_minute_interval` 以及没有精确秒数时的分钟兜底规则不是本需求对象，当前窗口保持不变。
- 非 `clock_only` 的历史 `score_transition` 路径复用了部分 `OCR_EXACT_WINDOW_*` 配置，不能通过全局替换数值的方式修改，否则会改变旧链路行为。
- 已经生成完成的历史 GIF 不重新编码；已经进入定位或编码的旧任务第一版不强行迁移，避免处理中途改变任务契约。

### 6.2 输出规则

| 事件/定位情况 | 输出窗口 | 说明 |
|---|---:|---|
| 有精确秒数，OCR 直接找到目标秒 | 前 20 秒 + 后 20 秒 | 约 40 秒，定位诊断为 `exact` |
| 只有单帧读到目标秒 | 前 20 秒 + 后 20 秒 | 允许使用，仍按现有规则标记 `estimated` |
| 目标附近读数推算出锚点 | 前 20 秒 + 后 20 秒 | 窗口缩短，但仍保留 `estimated`/`projected` 标记 |
| 有稳定映射并推算出锚点 | 前 20 秒 + 后 20 秒 | 不把推算结果伪装成直接读数 |
| 只有分钟，没有精确秒数 | 保持现状 | 不改分钟边界和分钟间隔规则 |
| 分钟级兜底或无法进入精确秒级定位 | 保持现状 | 不因本优化扩大失败范围 |
| 普通默认 GIF、T-DEED、非 clock-only 历史路径 | 保持现状 | 与本需求完全隔离 |

边界处理：如果锚点距离视频起点不足 20 秒，则从可用起点开始；如果锚点之后暂时没有足够分片，则沿用当前等待、租约和失败语义，不伪造缺失内容，也不改变 OCR 搜索截止策略。

### 6.3 实施方案

1. 在 `vision_runtime.py` 增加或拆分“秒级进球专用 20/20 配置”，同时保留 legacy `score_transition` 使用的 30/30 配置；分钟边界和分钟兜底分支维持原值。
2. 确认 `_ocr_output_shape()` 只有 `match_clock_second` 的秒级进球分支使用新配置，避免把所有 `30.0` 统一替换成 `20.0`。
3. 检查 OCR 任务定位后的窗口持久化逻辑，确保数据库和实际 FFmpeg 请求都收到 `20/20`，而不是只修改界面显示。
4. 评估是否同步修改 `event_driven_pipeline.py` 中新任务/恢复任务的初始 `30/30` 元数据：如果同步修改，必须保证旧任务恢复不被重写；如果暂不修改，定位完成后仍必须覆盖为 `20/20`，界面可在定位前短暂显示旧初始值。
5. 更新 `dashboard_server.py` 的展示逻辑，区分“秒级进球约 40 秒”和“分钟规则/旧路径的原有时长”，不能用一个固定文案误导用户。
6. 增加针对精确秒、单帧秒、估算秒、推算秒、分钟级和旧 `score_transition` 的回归测试；测试应同时断言持久化窗口和最终编码请求范围。
7. 使用已有保存视频做回放，比较窗口长度、GIF 可播放性、事件前后画面完整度、编码耗时和文件大小；确认后再决定是否部署到服务器。

### 6.4 预期效果

- 秒级进球 GIF 从约 60 秒缩短到约 40 秒，编码帧数理论上减少约三分之一。
- FFmpeg 编码时间、GIF 文件大小和磁盘写入量通常下降；后滚等待在需要等待后续画面时最多可减少约 10 秒。
- 缓存租约需要保护的后续时间缩短，但不会提升 OCR 找锚点的成功率，也不会修复已删除的视频分片、直播源中断或 PaddleOCR 推理超时。
- 由于只改变最终输出窗口，比赛时钟搜索范围、重扫次数、截止时间、缓存保留时间、直播重连和发布逻辑均不应发生变化。

### 6.5 必须覆盖的测试

- `match_clock_second` 返回 `before=20.0`、`after=20.0`，总时长约 40 秒。
- `exact`、单帧 `estimated`、附近估算 `estimated`、稳定映射 `projected` 都使用 20/20，且诊断标签不变。
- `match_clock_minute_boundary`、`match_clock_minute_interval`、分钟兜底仍保持既有窗口。
- legacy `score_transition` 仍保持 30/30，不受秒级配置影响。
- OCR 定位后持久化字段、编码调用参数和 Dashboard 展示彼此一致。
- 视频起点或终点不足 20 秒时正确裁剪，不产生负时间、不越过可用分片，也不改变既有失败语义。
- 服务重启后，已存在的旧任务不会被强制改成 20/20；新任务定位后使用 20/20。
- 默认 GIF、T-DEED、草稿队列、正式发布和事件去重相关回归测试通过。

### 6.6 验收标准、风险与回滚

- **验收标准**：新产生的秒级进球 OCR GIF 在任务记录和实际媒体范围上均为约 `-20/+20`；分钟级、默认 GIF、T-DEED 和 legacy 路径行为不变；现有测试和保存视频回放通过。
- **主要风险**：窗口缩短可能少掉进球前后的边缘画面；任务尚未定位时的初始展示值与最终值可能短暂不同；错误地复用全局常量会影响旧路径。
- **控制措施**：只按 `location_kind=match_clock_second` 分支切换；保留独立的 legacy 常量；保留旧任务契约；先回放验证再上线。
- **回滚方式**：恢复秒级进球专用配置为 `30/30` 并重启 Worker；不删除历史 GIF、数据库任务记录或缓存，不涉及破坏性迁移。

## 7. OCR 目标媒体未到达时的增量唤醒等待

- **状态**：已实施；目标媒体等待已采用独立的 3 秒轻量唤醒策略，通用等待和 OCR 扫描边界保持不变
- **优先级**：高（针对当前 `ocr_target_media_not_arrived` 的等待效率优化）
- **范围**：仅 OCR 第二条 `clock_only` 链路在“已经建立时钟映射、但目标画面尚未进入本地 TS 缓存”阶段的等待与重扫调度；默认 GIF、T-DEED、文章发布、直播源重连和缓存清理策略不在本项内
- **对应错误**：`ocr_target_media_not_arrived`

### 7.1 这个错误实际表示什么

OCR 已经完成了一部分工作，并且通常已经知道“目标比赛时间对应视频中的大概位置”，但本地缓存的 TS 分片还没有下载到目标位置。此时不能立即裁剪 GIF，因为目标画面还不存在；如果强行编码，只会得到缺画面、过短或无法播放的文件。

这不是“时钟一定识别失败”，也不是“直播源一定断了”。它可能只是直播存在正常延迟：事件发生后，目标画面还在持续写入缓存，稍后就会到达。只有在等待期间缓存长期不增长、超过硬截止时间，或目标位于永久缺口时，才会进一步转成失败。

### 7.2 当前实现和主要问题

当前已经具备目标媒体未到达状态、TS 尾部增长检测、等待状态持久化、媒体停滞判断和整场 OCR 硬上限，但远端目标与当前 TS 尾部之间的等待仍可能按较长的固定间隔（约 10～30 秒）再次检查。

因此会出现以下体验：

- 目标分片其实刚刚到达，但系统还在睡眠，要等下一次固定检查才开始 OCR；
- 用户看到任务长时间停留在“处理中”，难以区分是在正常等直播追上，还是媒体已经停止；
- 在目标刚好落入当前尾部附近时，额外等待时间会放大整条链路的延迟；
- 如果每次检查都重新启动 PaddleOCR，又会把“等待媒体”误变成额外的推理开销。

### 7.3 已实施方案

1. **把长间隔等待改为轻量增量检查**：目标尚未到达时，以约 3 秒为一轮读取 TS 清单和最新尾部时间；只读取元数据和文件存在性，不执行完整 OCR。
2. **以 TS 尾部增长作为唤醒条件**：发现新分片或尾部时间向前推进时，立即重新判断目标是否已经进入连续可读的分片，不再固定等满 10～30 秒。
3. **目标进入可读范围后只启动一次 OCR**：确认目标附近的 TS 连续、文件存在且能被读取后，再启动目标窗口 OCR；等待阶段不重复启动 PaddleOCR，也不为每个新分片创建一个 OCR 进程。
4. **保留现有安全上限**：继续保留 `OCR_MEDIA_STALL_TIMEOUT_SECONDS=60` 的媒体停滞判断和 `OCR_EVENT_HARD_LIMIT_SECONDS=300` 的整场 OCR 硬截止；任何一个上限触发后都必须结束等待并给出明确原因。
5. **区分可恢复等待与不可恢复缺口**：
   - 目标历史已被缓存淘汰：仍报告 `ocr_search_history_evicted`，不无限等待；
   - 目标落在视频永久缺口或截止时仍未到达：到截止后终止；
   - TS 尾部仍在增长、目标只是尚未追上：继续等待；
   - 目标进入连续 TS：立即进入 OCR。
6. **持久化等待进度**：继续写入 `progressive_scan`，至少保留当前状态、最后检查时间、最近一次 TS 尾部、等待开始时间、媒体停滞累计时长和截止策略；服务重启后可恢复等待，不因重启重置上限或无限重试。
7. **增加可读诊断文案**：Dashboard 中同时展示“目标还差多少秒”“最近一次检查时间”“TS 是否仍在增长”“已等待多久”“距离 60 秒停滞上限/300 秒硬上限还有多久”，避免只显示一个专业错误码。

### 7.4 可以解决什么问题

- **减少无意义的固定等待**：目标画面一到达就能被下一轮轻量检查发现，通常可把原本额外的 10～30 秒等待压缩到约 0～3 秒级别。
- **降低 OCR 重复开销**：等待期间不启动 PaddleOCR，避免把 CPU/GPU 时间消耗在尚未具备目标画面的窗口上。
- **减少“看起来卡住”的情况**：任务状态会持续更新，用户能看出是在等新 TS、媒体停滞，还是已经超过硬截止。
- **提高正常直播延迟场景的成功率和及时性**：只要直播源仍在产生连续分片，目标最终进入缓存后即可继续完成 OCR 和 GIF 编码。
- **重启后行为更稳定**：等待进度和截止信息持久化后，服务重启不会把已经等待的时间清零。
- **保持两条 GIF 链路隔离**：默认 GIF 不会等待 OCR，也不会因为本项增加额外扫描；T-DEED 和发布链路不改变。

### 7.5 不能解决什么问题

- 不能恢复已经被缓存清理、且无法重新下载的历史 TS；这仍属于 `ocr_search_history_evicted`。
- 不能修复直播源彻底断流、上游没有继续产生新分片、网络长期不可达或分片损坏的问题。
- 不能保证没有记分牌、画面严重遮挡、画面切源或 PaddleOCR 无法读数时一定找到目标；这些仍可能进入 `ocr_clock_target_not_located` 或其他现有失败语义。
- 不能直接解决 PaddleOCR 单次推理本身超时；本方案只避免在媒体未到达阶段重复启动推理。推理超时需要单独的 OCR 超时/模型优化待办。
- 不能把永久视频缺口变成可用画面；为此无限等待会拖住任务，因此必须保留截止和停滞上限。

### 7.6 可能的弊端和风险

- **检查次数增加**：约每 3 秒读一次清单，会增加少量磁盘目录扫描、文件 `stat` 和状态写入开销；不应每轮做完整 OCR 或大范围视频读取。
- **任务持续时间可能变长**：直播源慢但持续增长时，任务会按既有上限继续等待，用户看到“处理中”的时间可能比立即失败更长；需要通过倒计时和阶段文案解释这是可恢复等待。
- **尾部时间异常可能误判**：清单顺序错乱、分片时间戳跳变或文件刚创建但尚未写完时，可能提前尝试读取；必须同时检查文件存在、连续性和可读性，失败后回到等待而不是立刻判定 OCR 失败。
- **并发状态写入变多**：多个比赛同时等待时，`progressive_scan` 更新频率上升，需避免锁竞争和过度日志；建议只在尾部变化、状态变化或固定节流间隔时持久化。
- **仍需占用缓存租约**：等待期间必须保护目标附近分片，可能暂时增加磁盘占用；租约到期、媒体停滞或硬截止时应按现有清理规则释放。
- **不能完全消除时延**：3 秒只是检查粒度，实际还会受分片生成、文件写入和调度器负载影响，因此效果不是“零延迟”。

### 7.7 安全边界与回滚

- 只改 `vision_runtime.py` 中 OCR 目标媒体未到达的等待/唤醒逻辑，并在 `test_vision_runtime.py` 增加回归测试；第一版不改 `event_driven_pipeline.py`、发布器、Dashboard API 和数据库表结构。
- 不删除或放宽 60 秒媒体停滞上限、300 秒整场硬上限、历史淘汰判断和永久缺口终止规则。
- 不让每个新 TS 分片触发一个 PaddleOCR 进程；始终采用“轻量检查 → 目标可读后单次 OCR”。
- 如线上发现检查开销、锁竞争或误判增加，可将轻量检查间隔恢复到原有 10～30 秒；历史任务状态、GIF 文件和数据库记录无需删除或迁移。

### 7.8 必须覆盖的测试和验收标准

- 目标未进入缓存时只做轻量检查，不调用 OCR；TS 尾部增长后继续等待。
- 新 TS 到达目标范围后，在下一轮检查立即进入一次 OCR，且不重复启动多个 OCR worker。
- TS 连续性不足、文件刚生成不可读时继续等待，变为可读后再 OCR。
- TS 连续 60 秒不增长时终止并记录媒体停滞原因。
- 等待累计达到 300 秒时终止，不会无限等待。
- 目标历史已淘汰、目标位于永久缺口时不会被误判为可恢复等待。
- 服务重启后恢复 `progressive_scan`，不重置已等待时间、不绕过硬上限。
- 默认 GIF、T-DEED、文章草稿/发布和直播重连相关回归测试保持通过。
- 真实保存视频或生产日志验证：比较目标到达后的额外等待时间、OCR 启动次数、CPU/内存占用、缓存租约时长和最终 GIF 成功率。

### 7.9 完成后的预期效果

正常直播延迟场景下，目标画面到达缓存后会更快开始 OCR，减少无意义的 10～30 秒空等；直播源确实停滞或目标不可恢复时，仍会在明确的 60 秒/300 秒边界内结束，不会出现无限处理中。该优化主要改善“等待策略和可观测性”，不能替代直播源稳定性、历史分片保留和 OCR 识别质量方面的独立优化。

## 8. 支持同时跟踪 20 场比赛的分阶段容量扩展

- **状态**：待实施；本次仅登记方案，未修改运行配置、并发逻辑或服务器
- **优先级**：高（同一时段可能有 10～20 场比赛时）
- **目标**：先保证最多 20 场比赛都能及时启动 Worker、持续保存直播 TS 并接收事件，再根据服务器实测资源逐步提高 OCR 和 GIF 编码吞吐量。
- **范围**：Dashboard 比赛准入、比赛 Worker、直播 TS 采集、全局重任务协调、常驻 OCR Worker、GIF 编码、容量监控和服务器配置；不改变事件识别、OCR 定位规则、GIF 时间窗口、发布接口或文章内容。
- **当前基线配置**：`GIF_MAX_CONCURRENT_MATCHES=8`、`GIF_MAX_CONCURRENT_HEAVY_TASKS=5`、`GIF_MAX_CONCURRENT_VISION_TASKS=2`、`GIF_VISION_WORKERS=2`、`GIF_OCR_PERSISTENT_WORKER=1`。

### 8.1 需要先区分的两种“同时处理”

1. **同时跟踪 20 场比赛**：20 个比赛 Worker 可以同时运行，每场持续下载直播 TS、轮询比赛事件和 shotmap。该能力主要由 `GIF_MAX_CONCURRENT_MATCHES` 控制。
2. **同时执行 20 个 OCR/GIF 重任务**：20 场恰好同时进球时，让 20 个 OCR 推理或 20 个 GIF 编码真正并行。该能力受全局 heavy/vision 槽位、常驻 OCR Worker 数量、CPU、内存、磁盘和网络共同限制。

本待办第一阶段只承诺前一种能力。允许 20 场进入系统不等于让 20 个重任务同时抢占机器；事件集中发生时，OCR 和 GIF 会进入受控队列。这样可以先解决“第 9 场以后根本没有 Worker、也没有完整 TS 缓存”的核心问题，同时避免一次配置调整把服务器压垮。

### 8.2 当前实现为什么可以扩到 20 场

- Dashboard 已通过 `GIF_MAX_CONCURRENT_MATCHES` 控制比赛 Worker 上限，代码没有把上限固定死在 8 场；把配置改为 20 后，可以容纳最多 20 个活跃比赛 Worker。
- 每场比赛通常会有一个 Python Worker 和一个 FFmpeg 采集进程。它们负责保存 TS 和轮询事件，本身不代表一直在执行 PaddleOCR。
- OCR 和 GIF 使用全局 `HeavyTaskCoordinator` 限流：GIF 占用 heavy 槽；OCR/视觉任务同时占用 heavy 槽和 vision 槽，并保留 GIF 可用容量，避免 OCR 完全堵住编码。
- 当前已使用 persistent OCR Worker，多个比赛复用已加载的 PaddleOCR 模型，避免每次任务重复加载模型。
- 但每种语言的常驻 OCR Worker 当前仍主要由一个处理线程消费请求；即使把 vision 槽位直接调到 20，底层模型也不会自然变成 20 路真正并行，只会增加等待和资源竞争。

### 8.3 第一阶段推荐配置：先让 20 场都能采集

服务器第一轮建议只调整为：

```text
GIF_MAX_CONCURRENT_MATCHES=20

GIF_MAX_CONCURRENT_HEAVY_TASKS=5
GIF_MAX_CONCURRENT_VISION_TASKS=2
GIF_VISION_WORKERS=2

GIF_OCR_PERSISTENT_WORKER=1
GIF_OCR_MAX_CLIENTS=32
GIF_OCR_WORKER_IDLE_SECONDS=900
```

各项作用：

- `GIF_MAX_CONCURRENT_MATCHES=20`：最多允许 20 场比赛 Worker 同时运行和保存直播缓存。
- `GIF_MAX_CONCURRENT_HEAVY_TASKS=5`：全局最多同时执行 5 个 OCR/GIF 重任务，先保持当前基线，防止 CPU 和磁盘瞬间满载。
- `GIF_MAX_CONCURRENT_VISION_TASKS=2`：其中最多 2 个任务进入 OCR/视觉阶段，避免 PaddleOCR 争抢资源。
- `GIF_VISION_WORKERS=2`：保持单个比赛进程内现有的视觉任务线程数，不盲目按比赛数放大。
- `GIF_OCR_PERSISTENT_WORKER=1`：继续复用常驻模型，减少重复初始化时间和内存波动。
- `GIF_OCR_MAX_CLIENTS=32`：允许来自 20 场比赛的 OCR 客户端连接和排队，第一阶段无需创建 20 个模型副本。
- `GIF_OCR_WORKER_IDLE_SECONDS=900`：繁忙比赛时段保持 OCR Worker 常驻，避免短暂无任务后退出并重新加载。

#### 第一阶段预期效果

- 同时开始 10～20 场比赛时，最多 20 场可以各自拥有真实 Worker，不再因为原先 8 场上限导致后续比赛只出现在赛事列表、却没有持续采集 TS。
- 各比赛的直播历史更早开始保存，降低事件到达后才启动导致目标画面缺失的概率。
- 平时事件错峰发生时，现有 5 个 heavy 槽和 2 个 vision 槽通常可以继续处理。
- 多场同时产生事件时，OCR/GIF 仍会排队，处理延迟可能增加，但服务器资源更可控，不应因为强行 20 路推理而整体失去响应。

### 8.4 第二阶段候选配置：实测有余量后提高重任务吞吐

只有第一阶段稳定运行并收集高峰指标后，才评估例如：

```text
GIF_MAX_CONCURRENT_MATCHES=20
GIF_MAX_CONCURRENT_HEAVY_TASKS=8
GIF_MAX_CONCURRENT_VISION_TASKS=4
GIF_VISION_WORKERS=4

GIF_OCR_PERSISTENT_WORKER=1
GIF_OCR_MAX_CLIENTS=64
GIF_OCR_WORKER_IDLE_SECONDS=1800
```

这组值不是默认承诺，也不能未经压力测试直接用于生产。它的作用是允许更多 GIF 编码和 OCR 请求同时进入执行阶段，减少高峰排队；代价是更高的 CPU、内存、磁盘读写和进程调度压力。若底层仍只有一个 OCR 模型消费线程，仅提高外层 vision 槽位的收益可能有限，因此必须结合实际队列和 OCR 吞吐判断。

### 8.5 如果要求 20 场 OCR/GIF 也高度并行

仅修改环境变量不够，还需要单独设计下一阶段架构：

1. 启动多个常驻 OCR Worker 副本，而不是让所有请求只进入一个模型处理线程。
2. 增加 OCR 请求分发和负载均衡，保证同一任务的响应能正确回到对应比赛。
3. 明确每个 Worker 的并发、队列长度、超时、崩溃重启和健康检查语义。
4. 对 GIF 编码和 OCR 分别设置资源池，防止 OCR 挤占所有 CPU 后导致已经定位成功的 GIF 无法编码。
5. 评估 GPU 推理或增加 CPU 核数；关闭 oneDNN 的服务器尤其要通过真实样本测量 OCR 耗时。
6. 评估 SQLite 协调器在高频排队、续租和状态写入时的锁竞争，必要时再优化存储层，但第一阶段不提前改数据库架构。

每增加一个完整 PaddleOCR 模型副本，按当前本地观测可能增加约 1.5GB 级别常驻内存，实际数值必须以服务器环境为准。因此“20 场比赛”不应机械对应“20 个 OCR 模型”。

### 8.6 服务器资源和外部依赖门槛

实施前至少检查：

```bash
nproc
free -h
df -h
ps -eo pid,%cpu,%mem,rss,args
```

需要重点确认：

- **CPU**：20 个采集/Worker 进程加 OCR、FFmpeg 编码不能让 CPU 长期满载；还要关注 load average，而不只是瞬时 CPU 百分比。
- **内存**：必须容纳 Dashboard、20 个比赛 Worker、FFmpeg、常驻 OCR 模型和编码峰值；多个 OCR 副本会显著增加常驻内存。
- **磁盘**：当前每场保留约 900 秒 TS。按本地曾出现约 384MB/场估算，20 场仅 TS 可能约 7.7GB，还要包含 GIF、日志、临时文件和波动空间；服务器建议至少预留 30～50GB 可用空间。
- **网络**：若单路直播约 2～5Mbps，20 场持续下载约需要 40～100Mbps，并应预留协议和重连开销。
- **上游限制**：确认直播源是否允许同一服务器建立 20 路连接，事件和 shotmap 接口是否存在频率限制；本地并发配置无法修复上游限流或源不稳定。
- **OCR 性能差异**：服务器是否关闭 oneDNN、CPU 型号和 OCR 依赖版本都会影响吞吐，不能用本地速度直接推断服务器容量。

### 8.7 分阶段实施步骤

1. **先加载已完成的 Worker 识别修复**：用户手动重启本地 Dashboard 后，确认系统进程不会再被误判为旧比赛 Worker，`active_match_count` 与真实 Worker 一致。
2. **本地只做小规模回归**：本地机器不直接长期跑 20 场；验证 7～8 场准入、退出、重启恢复、TS 保存和任务统计没有回归。
3. **记录服务器基线**：在改配置前记录 CPU、内存、磁盘、网络、活跃比赛数、heavy/vision 队列长度和 OCR/GIF 平均耗时。
4. **服务器启用第一阶段配置**：只把比赛上限改为 20，heavy/vision 仍保持 `5/2/2`，按照项目固定服务器更新流程只重启 8899 Dashboard。
5. **先观察真实高峰**：至少覆盖一个 10 场以上同时开赛时段，不在观察过程中连续放大多个参数。
6. **判断瓶颈归属**：比赛未启动看准入和 Worker；TS 不增长看直播源/FFmpeg/网络；任务排队看 heavy/vision 槽；OCR 内部等待看常驻 Worker；编码慢看 CPU/磁盘。
7. **一次只提高一层容量**：资源有余量且队列持续增长时，先小幅提高 heavy，再评估 vision；每次调整后重新观察，不直接从 `5/2` 跳到 `20/20`。
8. **确有需要再做多 OCR Worker 架构**：只有单常驻 OCR Worker 被证明是稳定瓶颈时，才进入代码改造、独立压力测试和灰度上线。

### 8.8 监控指标和验收标准

需要观察 Dashboard/API 和 `output_gifs/dashboard/heavy_task_coordinator.json`，重点记录：

- `active_match_count` 是否等于实际应该运行的比赛数，并能在 20 场以内全部启动。
- `max_concurrent_matches` 是否为 20，`available_worker_slots` 是否按真实比赛数变化。
- `waiting_capacity` 是否只在已经达到 20 场时出现；低于 20 场时出现则属于异常。
- `heavy_tasks.queued`、vision 队列及 `oldest_wait_seconds` 是否只在事件高峰短暂上升，并能在高峰后回落。
- OCR 推理超时、任务硬超时、GIF 编码失败、直播源重连和 TS 缺口是否比调整前增加。
- CPU、load average、内存、swap、磁盘可用空间、磁盘读写延迟和网络吞吐是否保持在服务器可承受范围。
- Dashboard 健康接口、页面刷新、比赛停止和 Worker 清理在高峰期是否仍然正常。

第一阶段验收通过至少需要：20 场以内不再因比赛名额不足而缺少 Worker；TS 能持续保存；任务队列在高峰后可以消化；没有出现持续资源打满、Dashboard 失联、批量 OCR 超时或大量 GIF 编码失败。

### 8.9 可以解决、不能解决及主要弊端

#### 可以解决

- 原来超过 8 场后，部分比赛无法启动真实 Worker、无法从开赛阶段持续保存 TS 的容量问题。
- 因 Worker 启动过晚导致早期事件附近 TS 不存在的一部分失败。
- 通过受控队列让 20 场共享有限 OCR/GIF 能力，避免所有重任务无上限并发。

#### 不能直接解决

- 不能保证 20 场同时进球时，20 个 OCR/GIF 都立即完成；第一阶段会排队。
- 不能修复直播源断流、TS 损坏、上游限流、网络不足、OCR 看不清或历史已淘汰的问题。
- 不能仅靠增大 `GIF_MAX_CONCURRENT_VISION_TASKS` 获得等比例 OCR 加速；单常驻模型仍可能是串行瓶颈。
- 不能在没有服务器规格和高峰监控数据时保证第二阶段 `8/4/4` 一定稳定。

#### 主要弊端和风险

- 20 路直播会持续增加网络、磁盘写入、文件数量、轮询请求和进程数，即使没有进球也有基础成本。
- 事件集中时排队时间会增加，任务可能更晚完成；若超时仍从排队前开始计算，需要后续检查并区分排队超时和实际执行超时。
- 错误地把比赛、heavy、vision 和每进程线程数全部改为 20，可能造成 CPU 抢占、内存不足、磁盘抖动、OCR 超时和整个 Dashboard 失联。
- 多个 OCR Worker 会增加部署、路由、状态恢复和内存管理复杂度，不属于第一阶段的低风险配置调整。

### 8.10 回滚方案

- 第一阶段如出现资源不足，只需把 `GIF_MAX_CONCURRENT_MATCHES` 恢复为 8，并按固定流程重启 8899 Dashboard；不删除数据库、TS、GIF、OCR 环境或历史任务。
- heavy/vision 参数每次独立调整并独立回退，不与比赛上限绑成一次不可分割的变更。
- 回滚前保留故障时的队列状态、系统资源和日志证据，便于判断瓶颈；不得使用 `git reset --hard`、`git clean -fd` 或重建 OCR 环境处理容量问题。
- 已经启动的比赛如何在重启后恢复，以现有持久化和恢复逻辑为准；上线前必须先验证服务重启不会制造重复 Worker。

### 8.11 待办完成条件

- 服务器配置允许并准确显示最多 20 场活跃比赛，20 场以内均有匹配的真实 Worker 和持续增长的 TS。
- 第一阶段 `20/5/2/2` 通过真实高峰观察，资源未持续打满，Dashboard 和任务恢复正常。
- OCR/GIF 队列、等待时间、失败原因和系统资源均可查看，能够判断是比赛名额、直播采集、全局槽位还是 OCR Worker 形成瓶颈。
- 根据观测数据作出明确结论：保持第一阶段配置、提高至经过验证的第二阶段值，或进入多 OCR Worker 架构改造。
- 默认 GIF、OCR GIF、T-DEED、文章草稿/发布、事件去重、比赛停止和服务重启相关回归测试保持通过。
