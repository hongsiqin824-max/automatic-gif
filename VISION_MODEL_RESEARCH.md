# 足球视觉精定位模型调研与本地验证

## 本轮结论

足球专项模型已经在现有比赛录像上跑通。当前最值得继续验证的是
`T-DEED SoccerNet_small`，而不是早期的比分牌方案，也不是
`SoccerNetBall` 版本。

在 3 个含进球的 120 秒窗口和 3 个普通比赛窗口上，以 `Goal >= 0.20`
为候选阈值：

| 窗口 | 人工动作点 | 模型定位点 | 误差 | Goal 置信度 |
| --- | ---: | ---: | ---: | ---: |
| goal_1 | 1049.0 s | 1046.6 s | -2.4 s | 0.967 |
| goal_2 | 5033.0 s | 5034.4 s | +1.4 s | 0.363 |
| goal_3 | 6563.0 s | 6560.6 s | -2.4 s | 0.225 |
| negative_1 | 无进球 | 无 Goal 候选 | - | - |
| negative_2 | 无进球 | 无 Goal 候选 | - | - |
| negative_3 | 无进球 | 无 Goal 候选 | - | - |

这是一个可行性结果，不是准确率承诺。样本只有一场比赛、3 个进球，负样本也
不是整场扫描。当前结果足以说明“事件接口给粗时间，足球模型在短窗口内找秒级
位置”这条链路能跑通，但还不足以直接上线无人值守发布。

机器可读结果位于
`tmp/verification/tdeed_evaluation_report.json`，原始模型输出位于
`tmp/verification/tdeed_*_25fps_398/results_inference.json`。

## 为什么选择 T-DEED SoccerNet

`T-DEED` 是针对体育视频事件时序定位的模型。它直接读取一段连续画面，并在
时间轴上输出事件类别与置信度。官方 `SoccerNet` 检查点包含：

- Goal
- Yellow card
- Red card
- Yellow->red card
- 射正、射偏、犯规、角球、换人等其他足球事件

`SoccerNetBall` 的重点是传球、带球、头球等密集球权动作。虽然也有 Goal 类，
但没有完整的黄牌、红牌类别，因此不适合作为本项目的主模型。

## 本地运行规格

- 官方仓库：<https://github.com/arturxe2/T-DEED>
- 检查点：官方 `SoccerNet_small/checkpoint_best.pt`
- 输入分析流：25 FPS、398x224、无音频
- 模型窗口：100 个采样帧；SoccerNet 推理每 12 帧取一帧
- 本次外层候选窗口：120 秒
- 运行设备：Apple Silicon CPU；当前 Python 环境无法使用 MPS
- 单任务端到端耗时：13.64 至 17.54 秒，中位数 13.82 秒

分析用低分辨率视频只服务于定位。最终 GIF 必须回到 RTMP 原始缓存，根据模型
输出的媒体时间戳从原画质画面截取，因此不会把 398x224 的分析画面用于发布。

三个负窗口并发运行时，每个任务约 38 秒，原因是它们争用同一 CPU。这说明正式
实现需要限制并发，或者使用 NVIDIA GPU。第一阶段只处理一场比赛时，单任务串行
推理仍有机会满足一分钟目标；正式 SLA 必须在部署机器和真实 RTMP 上重测。

## CALF 对照结果

同一批 3 个进球窗口、3 个普通窗口上，官方 SoccerNet CALF 检查点只命中第 3
个进球：召回 1/3，未在 3 个负窗口产生 Goal 误报。其预先提取特征后的模型推理
约 10 秒，但特征提取另有成本。

因此 CALF 可以保留为研究基线，不建议作为当前主定位器。

## 正式链路中的位置

```text
事件接口返回 G / YC / RC 和大概比赛时间
  -> 时间同步层映射到 RTMP 缓存的大概位置
  -> 从缓存取 60 至 120 秒候选窗口
  -> T-DEED 只读取低分辨率分析流
  -> 只保留与接口事件类型一致的候选类别
  -> 在候选中选择置信度和时间距离最合适的锚点
  -> 回到原始 RTMP PTS 精确截取
  -> FFmpeg 生成 GIF
```

事件接口仍是事实源。模型不能把接口的进球改判成红牌，也不能凭视觉结果覆盖
接口返回的人物。模型的职责只有两个：在接口粗时间附近找画面，以及给出复验
置信度。

## 红黄牌现状

模型类别层面支持黄牌、红牌和两黄变红，但当前录像没有经过确认的真实出牌标注，
因此本轮不能证明出牌定位准确。第二个进球窗口中出现了多次卡片候选，说明卡片类
需要单独校准阈值，不能直接复用进球的 `0.20` 阈值。

开始红黄牌验证至少需要：

- 含真实举牌画面的比赛录像；
- 每次举牌的大致视频时间；
- 区分首次出牌、转播回放和赛后字幕；
- 黄牌、直接红牌、两黄变红分别有样本。

## 许可证与部署风险

- T-DEED 代码仓库使用 GPL-3.0。
- 官方权重通过公开 Google Drive 提供，但仓库没有给权重单列商业许可。
- CALF 代码为 Apache-2.0。
- SoccerNet 数据本身也有单独的数据访问与使用条款。

在把 T-DEED 代码或权重放入正式商业服务前，需要公司法务或合规人员确认使用
方式。技术验证可以继续，但不应把“公开可下载”等同于“已确认可商用”。

## 下一步验收条件

1. 收集至少 10 场、30 个进球，包含不同联赛、转播方、分辨率和无比分牌画面。
2. 每个进球标注动作锚点以及理想 GIF 起止点，并随机抽取足够负窗口。
3. 按固定阈值统计召回、误报、定位误差 P50/P95 和推理耗时 P95。
4. 单独建立黄牌、红牌、两黄变红评测集，分别选择阈值。
5. 达标后封装 `vision_locator.py`，接入 `event_driven_pipeline.py`，替代固定
   `event_to_video_offset` 截取。
6. 真实 RTMP 到位后，再测接口延迟、直播延迟、时间漂移和事件后 60 秒 SLA。

